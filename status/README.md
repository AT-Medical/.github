# Status

This directory stores repository health and operational status information.

## Structure

| Directory | Purpose |
|-----------|---------|
| `generated/` | Auto-generated status reports from CI workflows |

## Notes

Files in `status/generated/` are created automatically by the `repository-selfcheck` workflow and should not be edited manually.
